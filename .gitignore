__pycache__/
*.py[cod]
.pytest_cache/
.venv/
node_modules/
.npm-cache/
.data-app-offline/
hybrid_rollout/report_site/app/dist/
runtime/
private/
private_runtime/
auth.json
*.key
*.pem
*.sqlite*
*.db
.env
.env.*
!.env.example
*.log
.DS_Store

# Run artifacts: audit and monitor logs are append-only records of a session,
# not source. Always pass --monitor-audit to put them somewhere deliberate;
# these patterns stop a default-path run dirtying the tree.
monitor_shadow.jsonl
*_audit.jsonl
live_loop_audit.jsonl
phase*_packets.jsonl
phase*_astra.jsonl
phase*_dryrun.jsonl
